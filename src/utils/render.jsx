import React from 'react';
import ReactDOM from 'react-dom';

export default function render(Component, ) {
    ReactDOM.render(
        <AppContainer>
            <Component/>
        </AppContainer>,
        root
    );
};